// formkit.config.ts
import { info, warning } from '@formkit/icons'
import { defineFormKitConfig } from '@formkit/vue'

export default defineFormKitConfig({
  icons: {
    info, warning,
  }
})
